package com.JavaAlgos.Jose;

public class SLLCycleCheck {
    /**
     * The way you do this is start with two pointers.
     * P1 = Slow
     * P2 = Fast (move forward by two)
     * Then you keep moving the two pointers along
     * If P2 == null then there is no cycle
     * If P2 == P1 then there is a cycle
     * */
}
